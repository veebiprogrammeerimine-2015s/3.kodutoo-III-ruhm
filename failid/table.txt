CREATE TABLE users(
id INT NOT NULL AUTO_INCREMENT PRIMARY KEY,
email VARCHAR(50),
password VARCHAR(50),
created TIMESTAMP
);

CREATE TABLE armorid(
id INT NOT NULL AUTO_INCREMENT PRIMARY KEY,
user_id INT NOT NULL,
type VARCHAR(20),
race VARCHAR(20),
color VARCHAR(20),
deleted DATE,
FOREIGN KEY (user_id) REFERENCES users(id)
);