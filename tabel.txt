CREATE TABLE creatednames(
id INT NOT NULL AUTO_INCREMENT PRIMARY KEY,
	user VARCHAR(255) NOT NULL,
	email VARCHAR(128) NOT NULL,
	password VARCHAR(128),
	created TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
	UNIQUE(email),
	UNIQUE(user)
);
